# 👋 Welcome to aiplang — start here

Hey! Welcome to the **aiplang** community 🐴

## What is aiplang?

aiplang is a web language designed to be generated by AI (Claude, GPT) — not written by humans.

A single `.aip` file = complete app with frontend + backend + database + auth.

```aip
~db sqlite ./app.db
~auth jwt $JWT_SECRET expire=7d

model User {
  id       : uuid : pk auto
  email    : text : required unique
  password : text : required hashed
}

api POST /api/auth/login {
  $user = User.findBy(email=$body.email)
  ~check password $body.password $user.password | 401
  return jwt($user) 200
}

%home dark /
~theme accent=#6366f1 radius=1rem font=Inter
nav{MyApp>/login:Sign in}
hero{Welcome|AI-generated in 30 seconds.} animate:blur-in
foot{© 2025}
```

## Get started

```bash
# Install
npm install -g aiplang

# Create project
npx aiplang init my-app

# Run
cd my-app && npx aiplang serve
```

Opens at `http://localhost:3000` with hot reload.

## Key links

| | |
|---|---|
| 📦 npm | `npm install -g aiplang` |
| 📖 GitHub | https://github.com/isacamartin/aiplang |
| 🧠 Prompt Guide | How to generate great apps with Claude |
| 📝 Templates | `/templates` folder in the repo |
| 🔒 Security | SECURITY.md |

## Discussion categories

- **💬 General** — general questions and conversations
- **💡 Ideas** — feature requests and suggestions
- **🙏 Q&A** — technical questions
- **🎉 Show & Tell** — share what you built

---

Welcome! If you have questions, open a Q&A thread. If you built something, head to Show & Tell! 🚀
